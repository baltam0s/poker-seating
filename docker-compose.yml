version: "3.9"

services:
  poker:
    build: .
    container_name: poker-seating
    ports:
      - "6006:3000"      # Host 6006 -> container 3000
    volumes:
      - poker-data:/app/data   # only persist DB
    restart: unless-stopped

volumes:
  poker-data:
