version: '3.8'

services:
  poker-seating:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - poker-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000

volumes:
  poker-data: